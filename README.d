# DoAnLapTrinhWeb
Cách tải toàn bộ project về máy bằng Git (Clone bằng HTTPS)

Nếu bạn muốn tải toàn bộ mã nguồn của dự án về máy để chạy hoặc chỉnh sửa, bạn có thể sử dụng lệnh git clone.

📌 Bước 1: Cài Git (nếu chưa có)

Tải Git tại: https://git-scm.com/downloads

Cài đặt bình thường → mở Git Bash.

📌 Bước 2: Clone dự án bằng HTTPS

Chạy lệnh sau trong Git Bash:

git clone https://github.com/duywadeptry1/DoAnLapTrinhWeb.git

📌 Bước 3: Mở dự án

Sau khi clone xong, vào thư mục:

cd DoAnLapTrinhWeb


Rồi mở trong VS Code hoặc Visual Studio:

VS Code:

code .


Visual Studio: mở file .sln trong thư mục dự án.

📌 Ghi chú

Clone bằng HTTPS là cách phổ biến nhất để tải project.

Bạn không cần tài khoản GitHub để tải dự án bằng HTTPS.

Nếu muốn push code, bạn phải đăng nhập GitHub hoặc dùng Personal Access Token.
